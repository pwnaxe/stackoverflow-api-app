import React from 'react';
import CustomizedDialogs from '../components/Contact';

export default {
  title: 'CustomizedDialogs',
  component: CustomizedDialogs,
};

const Template = (args) => <CustomizedDialogs {...args} />;
export const Default = Template.bind({});




